language: r
sudo: false
cache: packages

r:
  - oldrel
  - release
  - devel  
before_install:
  - tlmgr install index
github_packages:
  - DescriptiveStats.OBeu
  - devtools
r_packages:
  - forecast
  - foreach
  - jsonlite
  - locfit
  - purrr
  - rjson
  - rlist
  - stats
  - TimeSeries.OBeu
  - trend
  - tseries
  - utils
apt_packages:
  - libxml2-dev
sudo: required
warnings_are_errors: false
after_success:
- Rscript -e 'covr::codecov()'